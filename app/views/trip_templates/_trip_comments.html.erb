<div class="card">
  <div class="card-header d-flex align-items-center justify-content-between">
    Comments

    <a data-toggle="collapse" href="#trip_comments_new_form">
      Add comment
    </a>
  </div>

  <ul class="list-group list-group-flush">
    <% @trip.comments.each do |comment| %>
      <li class="list-group-item d-flex align-items-center justify-content-between">
        <a href="/comments/<%= comment.id %>">
          <%= comment.body %>
        </a>

        <%if current_user == comment.commenter %>
          <a href="/delete_comment_from_trip/<%= comment.id %>">
            Delete
          </a>
        <% end %>

      </li>
    <% end %>

    <div class="card-footer">
      <div class="collapse" id="trip_comments_new_form">
        <%= render partial: "trip_templates/trip_comments_new_form" %>
      </div>
    </div>

  </ul>
</div>

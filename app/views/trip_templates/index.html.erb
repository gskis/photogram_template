<div class="row mb-3">
  <div class="col-md-12">
    <h1>
      All trips
    </h1>

    <a href="/trips/new" class="btn btn-block btn-outline-secondary">
      Add a new trip
    </a>
  </div>
</div>

<!-- Ransack Code start -->
<div class="row">
  <div class="col-md-12 mb-3">
    <%= render partial: "trip_templates/trips_search_form" %>
  </div>
</div>
<!-- Ransack Code end -->

<!-- Location Map Code start -->
<div class="row mb-3">
  <div class="col-md-12">
    <%= render partial: "trip_templates/all_photos_map" %>
  </div>
</div>
<!-- Location Map Code end -->

<hr>

<div class="row mb-3">
  <div class="col-md-12">
    <table class="table">
      <tr>
        <th>
          ID
        </th>

        <th>
          Country name
        </th>

        <th>
          Image
        </th>

        <th>
          Owner
        </th>

        <th>
          City visited
        </th>

        <th>
          Rating
        </th>

        <th>
          Must do
        </th>

        <th>
          Created at
        </th>

        <th>
          Updated at
        </th>

        <th>
        </th>
      </tr>

      <% @trips.each do |trip| %>
      <tr>
        <!-- Display trip.id start -->
        <td>
          <%= trip.id %>
        </td>
        <!-- Display trip.id end -->

        <!-- Display trip.country_name start -->
        <td>
          <%= trip.country_name %>
        </td>
        <!-- Display trip.country_name end -->

        <!-- Display trip.image start -->
        <td>
          <img src='<%= trip.image %>' class='img-fluid'>
        </td>
        <!-- Display trip.image end -->

        <!-- Display trip.owner_id start -->
        <td>
          <% if trip.owner.present? %>
            <a href="/users/<%= trip.owner.id %>">
              <%= trip.owner.username %>
            </a>
          <% end %>
        </td>
        <!-- Display trip.owner_id end -->

        <!-- Display trip.city_visited start -->
        <td>
          <%= trip.city_visited %>
        </td>
        <!-- Display trip.city_visited end -->

        <!-- Display trip.rating start -->
        <td>
          <%= trip.rating %>
        </td>
        <!-- Display trip.rating end -->

        <!-- Display trip.must_do start -->
        <td>
          <%= trip.must_do %>
        </td>
        <!-- Display trip.must_do end -->

        <!-- Display trip.created_at start -->
        <td>
          <%= time_ago_in_words(trip.created_at) %> ago
        </td>
        <!-- Display trip.created_at end -->

        <!-- Display trip.updated_at start -->
        <td>
          <%= time_ago_in_words(trip.updated_at) %> ago
        </td>
        <!-- Display trip.updated_at end -->

        <td>
          <a href="/trips/<%= trip.id %>">
            Show details
          </a>
        </td>
      </tr>
      <% end %>
    </table>

    <div class="row mb-3">
      <div class="col-md-12">
        <%= paginate @trips, theme: "twitter-bootstrap-4" %>
      </div>
    </div>

  </div>
</div>
